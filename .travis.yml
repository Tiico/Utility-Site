language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm test
  - npm run build
deploy:
  provider: heroku
  app: utility-app
  local_dir: public
  on:
    repo: Tiico/Utility-Site
    branch: master